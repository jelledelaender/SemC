<?php
	
	function performExtensionTest($test) {
		
		if ($test == "yourCustomTest") {
		      // $status:
		      // $data (for example: single number
            throwStatus($status, $data);
		}
		
		// Add other custom tests if wwnted, see test above as example //
		
	}
	
?>